public static class ProjectHorizonGenerator
{
    // Increment deliberately whenever deterministic world-generation output changes.
    // Golden-seed tests bind their expected checksums to this version.
    public const int Version = 2;
}
